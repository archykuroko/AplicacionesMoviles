<resources>

    <!-- Píldora rellena (activo) -->
    <style name="Widget.Cyberdream.NeonPill" parent="Widget.Material3.Button">
        <item name="useMaterialThemeColors">false</item>   <!-- 🔧 clave -->
        <item name="cornerRadius">24dp</item>
        <item name="backgroundTint">@color/neon_cyan</item>
        <item name="rippleColor">@color/neon_cyan</item>
        <item name="strokeWidth">0dp</item>

        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/bg_oscuro</item>

        <item name="iconPadding">8dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

    <!-- Píldora contorno (inactivo) -->
    <style name="Widget.Cyberdream.NeonPill.Outlined" parent="Widget.Material3.Button.OutlinedButton">
        <item name="useMaterialThemeColors">false</item>   <!-- 🔧 clave -->
        <item name="cornerRadius">24dp</item>
        <item name="strokeColor">@color/neon_cyan</item>
        <item name="strokeWidth">2dp</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@color/neon_cyan</item>

        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/neon_cyan</item>

        <item name="iconPadding">8dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
    </style>

</resources>
